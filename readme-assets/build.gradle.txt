plugins {
    id 'java'
}

group = 'com.technicaltask'
version = '1.0-SNAPSHOT'

repositories {
    mavenCentral()
}

java {
    toolchain {
        languageVersion = JavaLanguageVersion.of(11)
    }
}

dependencies {
    implementation 'io.appium:java-client:9.1.0'
    implementation 'org.seleniumhq.selenium:selenium-java:4.18.1'
    implementation 'io.github.bonigarcia:webdrivermanager:5.7.0'
    testImplementation 'org.testng:testng:7.9.0'

    implementation 'org.springframework.boot:spring-boot-starter:2.7.3'
    testImplementation 'org.springframework.boot:spring-boot-starter-test:2.7.3'
}

test {
    useTestNG() {
        if (project.hasProperty('yummlyTest')) {
            suites './src/test/resources/suites/mobile/yummlyTest.xml'
        }
        if (project.hasProperty('xmTest')) {
            suites './src/test/resources/suites/web/xmTest.xml'
        }
    }
}